{
  "name": "wild-australia-evolution-clash",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check engine.js && node --check app.js && npm test"
  }
}
